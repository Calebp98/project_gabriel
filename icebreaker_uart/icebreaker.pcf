# iCEbreaker Pin Constraints File (PCF)
# For iCE40UP5K FPGA

# 12 MHz Clock
set_io CLK 35

# UART TX (to picoprobe GPIO 5 RX - connect to picoprobe GPIO 5)
# Using PMOD1A pin 2 (top row, second pin)
set_io TX 2

# On-board LED for debugging (heartbeat)
set_io LED1 11
